# Notebooks

Run notebooks through the unified dev environment:

```bash
make notebook
```

The example notebook under `notebooks/` is intentionally lightweight and relies on **`chem`** extras plus standard scientific Python packages.
